{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Notation</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ entity.valeur }}</td>
            </tr>
            <tr>
                <th>Idquestion</th>
                <td>{{ entity.idQuestion }}</td>
            </tr>
            <tr>
                <th>Idstagiaire</th>
                <td>{{ entity.idStagiaire }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('questionnaire_notation') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('questionnaire_notation_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}

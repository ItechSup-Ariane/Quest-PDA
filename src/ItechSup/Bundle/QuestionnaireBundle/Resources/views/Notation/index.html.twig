{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Notation list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Valeur</th>
                <th>Idquestion</th>
                <th>Idstagiaire</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('questionnaire_notation_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.valeur }}</td>
                <td>{{ entity.idQuestion }}</td>
                <td>{{ entity.idStagiaire }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('questionnaire_notation_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('questionnaire_notation_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('questionnaire_notation_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
